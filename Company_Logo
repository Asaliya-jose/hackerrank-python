import math
import os
import random
import re
import sys
from collections import Counter

if __name__ == '__main__':
    s = input()
    counts = Counter(sorted(s)).most_common()
    res = sorted(counts, key=lambda x: (-x[1], x[0]))

    for char, count in res[:3]:
        print(f"{char} {count}")
