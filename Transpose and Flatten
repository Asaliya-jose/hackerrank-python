import numpy
n, m = map(int, input().split())
matrix_list = []
for _ in range(n):
    matrix_list.append(list(map(int, input().split())))
my_array = numpy.array(matrix_list)
print(numpy.transpose(my_array))
print(my_array.flatten())
